These files are from the kissat project:
https://github.com/arminbiere/kissat/tree/master/test/cnf

The files are sorted to 'sat' and 'unsat' directories for testing purposes.
Otherwise they are unchanged.
